import React from 'react';
import { SectionHero } from '../../content';

const LibraryHero: React.FC = () => {
  return (
    <SectionHero
      title="Explore Your Wellness Journey"
      subtitle="Discover audio, movement, and reading to support calm, focus and balance."
    />
  );
};

export default LibraryHero;
